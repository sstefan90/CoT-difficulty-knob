# Avalon E1 — Llama 8B, Merlin, minimal, B=1024, N=30, shuffled roles
# Shuffled rerun of avalon_e1_merlin_n30.yaml (fixed-pos).
# Corrects evil-position pinning; Merlin assassination remains naive-random.

run_name: avalon_e1_merlin_min_bayesian

model:
  backend: sglang
  name: meta-llama/Meta-Llama-3.1-8B-Instruct
  base_url: http://localhost:30000
  temperature: 0.0
  think: false

llm_role: Merlin
llm_player_idx: 0
prompt_variant: minimal

budgets: [1024]
n_per_cell: 30
with_summarizer: true
summarizer_backend: anthropic
shuffle_all_roles: true
bot_strategy: bayesian
