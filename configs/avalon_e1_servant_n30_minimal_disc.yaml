# Avalon E1 — Llama 8B, Servant, minimal prompt, B=1024, WITH discussion, N=30
# Headline condition + discussion ablation: tests whether a public discussion
# statement before team votes improves good-side reasoning.
# Pair with avalon_e1_servant_n50_minimal.yaml (no discussion) for comparison.

run_name: avalon_e1_servant_minimal_disc

model:
  backend: sglang
  name: meta-llama/Meta-Llama-3.1-8B-Instruct
  base_url: http://localhost:30000
  temperature: 0.0
  think: false

llm_role: Servant
llm_player_idx: 0
prompt_variant: minimal

budgets: [1024]
n_per_cell: 30
with_discussion: true
with_summarizer: true
